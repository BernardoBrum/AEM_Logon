import styled from "styled-components";

const ErrorStyled = styled.h3`
  color: #e9b425;
  text-align: center;
  visibility: ${(props) => props.visibility};
  font-size: 16px;
`;

export { ErrorStyled };
